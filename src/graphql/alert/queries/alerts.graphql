query Alerts($locationId: Int!) {
    locationAlerts(locationId: $locationId) {
        id
        isEnabled
        message
        criteria
        comparator
        value
        updateFrequency
        createdAt
        updatedAt
    }
}
