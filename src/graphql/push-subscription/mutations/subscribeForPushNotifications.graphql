mutation SubscribeForPushNotifications($userAgent: String!, $endpoint: String!, $p256dh: String!, $auth: String!) {
    subscribeForPushNotifications(userAgent: $userAgent, endpoint: $endpoint, p256dh: $p256dh, auth: $auth) {
        id,
        createdAt,
        updatedAt
    }
}
